pipeline {
    agent any

    stages {
        stage('GitHub Integration Success') {
            steps {
                echo 'GitHub + Jenkins integration successful!'
            }
        }
    }
}